spring:
  application:
    name: timesheet
  data:
    mongodb:
      uri: mongodb://root:123@localhost:27017/
      database: mydb

server:
  port: 8082

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}